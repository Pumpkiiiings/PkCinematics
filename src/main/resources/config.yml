# ==========================================
# PkCinematics Main Configuration
# ==========================================

update-checker:
  # Should the plugin check for updates on startup?
  enabled: true
  
  # Notify operators when they join the server?
  on-join-notification: true
